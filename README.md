# Source Map Viewer

A visual tool for inspecting and debugging JavaScript source maps. Upload or paste a source map to see how generated code positions map back to original source, with pixel-accurate highlighting and interactive mapping connections.

## Features

- **Three input modes** — file upload (`.js`/`.map`), pasted text, or URL fetch
- **Interactive mapping viewer** — hover any mapped segment to see the original-to-generated connection via hand-drawn SVG curves
- **Syntax highlighting** — powered by Shiki with segment color overlay
- **Source map validation** — detects invalid source indices, out-of-bounds positions, and suspicious mappings
- **Coverage stats** — mapped vs unmapped byte counts per file
- **Shareable short URLs** — source map data stored in KV with 8-char short IDs, auto-renewed 30-day TTL on every access
- **LLM-friendly** — AI tools get a structured markdown debug report when fetching a shared URL (content negotiation via User-Agent / Accept headers)
- **AI debug prompt** — generates structured prompts for AI-assisted debugging
- **Dark mode** — automatic system detection with manual toggle

## AI-Friendly

Every shared URL works out of the box with AI coding tools like Claude Code and Codex. When an AI fetches a visualization URL, it receives a structured markdown debug report instead of HTML — no browser or JavaScript required.

**How it works:** The server detects AI requests via User-Agent patterns and the `Accept: text/markdown` header. Browsers get the normal interactive visualization.

Just paste a shared URL into Claude Code or Codex and it can read the full source map analysis:

```markdown
# Source Map Debug Report

## Problem

This source map has **1 bad mappings** out of 9 total.
The first bad mapping appears at generated position 1:33.

## Summary

- **Sources:** input.js
- **Total mappings:** 9
- **Bad mappings:** 1 (11%)
- **Names in source map:** a, b, console, log

## Original code

1 | const a = 1;
2 | const b = 2;
3 | console.log(a + b);

## Generated code

1 | var a = 1;
2 | var b = 2;
3 | console.log(a + b);

## Mappings (9)

| Gen  | Generated code | →   | Orig | Original code | Source   |     |
| ---- | -------------- | --- | ---- | ------------- | -------- | --- |
| 1:4  | `a = 1;`       | →   | 1:4  | `t a = 1;`    | input.js |     |
| 1:8  | `1;`           | →   | 1:8  | `= 1;`        | input.js |     |
| ...  |                |     |      |               |          |     |
| 1:33 |                | →   | 3:21 |               | input.js | ⚠️  |
```

The report includes original/generated code with line numbers, a full mapping table with bad mappings flagged, the raw VLQ string, and analysis prompts — everything an AI needs to diagnose source map issues.

This is the same report generated by the **AI Debug Prompt** button (`AiDebugPanel`) in the visualization toolbar. The difference is that sharing a URL lets AI fetch it directly, while the button copies the report to your clipboard for manual pasting.

## CLI

Install globally or use via `npx`:

```bash
npx source-map-viewer bundle.js           # open visualization in browser
npx source-map-viewer bundle.js --url     # print shareable URL
npx source-map-viewer bundle.js --ai      # print AI debug report to stdout
```

The CLI auto-detects source maps from inline `sourceMappingURL` comments, external `.map` references, or sibling `.map` files. You can also pass a `.map` file directly.

Pipe the AI report into your coding assistant:

```bash
npx source-map-viewer dist/bundle.js --ai | pbcopy
```

## Development

```bash
vp dev          # Start dev server
vp build        # Production build
vp check        # Format + lint + type-check
vp test --run   # Run tests
```

## Resources

- [ECMA-426: Source Map Format](https://tc39.es/ecma426/) — the official specification, covers VLQ encoding, `mappings` format, `sections` for index maps, and `sourcesContent`
- [source-map-js](https://github.com/7rulnik/source-map-js) — the parser used by this tool (performance-optimized fork of Mozilla's `source-map` 0.6)
- [Debug with Source Maps](https://developer.chrome.com/docs/devtools/javascript/source-maps/) — Chrome DevTools guide with visual examples
- [Generating Source Maps](https://pvdz.ee/weblog/281) — deep dive into VLQ encoding, how segments encode line/column deltas
- [Source Map Visualization (original)](https://evanw.github.io/source-map-visualization/) — Evan Wallace's original tool that inspired this project

## Tech Stack

Vue 3, Vite+, Void SDK (SSR on Cloudflare Workers), Shiki, UnoCSS, Cloudflare KV
