{
  "name": "source-map-viewer",
  "version": "0.1.0",
  "description": "Inspect and debug source map mappings from the terminal",
  "bin": {
    "source-map-viewer": "bin.js"
  },
  "files": [
    "bin.js",
    "dist"
  ],
  "type": "module",
  "scripts": {
    "build": "vp pack src/cli.ts --platform node --format esm --out-dir dist --deps.never-bundle open --deps.never-bundle source-map-js",
    "release": "bumpp && vp run --filter source-map-viewer build && vp pm publish --no-git-checks"
  },
  "dependencies": {
    "open": "^10.0.0",
    "source-map-js": "^1.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
